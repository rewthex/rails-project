<h1>All available tags</h1>
<ul>
<% @tags.each do |tag| %>
<li><%= link_to tag.name, tag_path(tag) %> <%= link_to "destroy", tag_path(tag) , method: :delete, data: {confirm: "Are you sure?" } %> </li>
<% end %>
</ul>
<p><%= link_to "<< Back to articles list", articles_path %></p>